{
    "navigationFallback": {
        "rewrite": "index.html",
        "exclude": [
            "/assets/*.{png,jpg,gif,ico,min.js,js}",
            "/*.{png,jpg,gif,ico}",
            "/*.{css,scss,js}",
            "/pdfjs/*.{js}"
        ]
    }
}