{
  "routes": [
    {
      "route": "/api*",
      "redirect": "https://mss.hospitalmetropolitano.org/api"
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": [
      "/assets/*.{png,jpg,gif,ico}",
      "/*.{png,jpg,gif,ico}",
      "/*.{css,scss,js}",
      "/pdfjs/*.{js}"
    ]
  }
}